name: test

on:

  push:
    branches: [ "main, dev" ]
  pull_request:
    branches: [ "main, dev" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: pyton install 
        run: python --version

      - name: test
        run: ls -la
      - name: test
        run: pwd